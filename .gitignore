# Zola build output (Cloudflare regenerates this on every deploy)
/public

# Claude Code working files
CLAUDE.md
.claude/

# Local planning / design notes — keep out of the public repo
/docs

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
